import productService from '../services/productService';
import { ADD_PRODUCT, DELETE_PRODUCT } from './actionTypes';


export const addProduct = (price, imageUrl, title, status, color, brand, category, description, isOfferable) => {

}
export const deleteProduct = (title) => {

}